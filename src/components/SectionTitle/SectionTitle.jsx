import React from "react";

function SectionTitle({ name }) {

    return (
        <div className="SectionTitle">
            <h2 className="SectionTitle__name">{name}</h2>
        </div>
    )
}

export default SectionTitle;